# GEO content pipeline

Topic clusters for generative-engine authority:

1. Viewer / Formatter / Validation
2. JSONPath
3. JQ tutorials & filters
4. API & webhook debugging

Ship answer-first pages under `/learn` (see `src/app/data/learnPages.ts`) and deepen with MDX blog posts in `content/blog/`.

Target cadence: ~20 unique pages/quarter until 100+. Prefer examples and cheatsheets over thin doorway pages.
